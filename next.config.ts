import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // produce a static export for Next 16+ (replaces deprecated `next export` CLI)
  output: 'export',
};

export default nextConfig;
