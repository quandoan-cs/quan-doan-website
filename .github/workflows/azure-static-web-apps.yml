name: "Disabled: autogenerated Azure Static Web Apps workflow (main)"

on:
  workflow_dispatch: {}

# This workflow was autogenerated by the Azure portal and previously triggered
# push events on `main`. It's intentionally disabled in favor of the custom
# `azure-static-webapps.yml` workflow that targets the `azure-build-deploy`
# branch.

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: Disabled
        run: echo "Autogenerated Azure SWA workflow (disabled). Use .github/workflows/azure-static-webapps.yml instead."
